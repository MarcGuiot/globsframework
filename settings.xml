<settings>
    <servers>
        <server>
            <id>github</id>
            <username>${env.GH_MAVEN_REGISTRY_USER}</username>
            <password>${env.GH_MAVEN_REGISTRY_ACCESS_TOKEN}</password>
        </server>
        <server>
            <id>github-marc</id>
            <username>${env.GH_MAVEN_REGISTRY_USER}</username>
            <password>${env.GH_MAVEN_REGISTRY_ACCESS_TOKEN}</password>
        </server>
    </servers>
</settings>
